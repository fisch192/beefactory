---
import type { Lang } from '../lib/i18n';
import { t } from '../lib/i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
---

<div class="bg-near-black text-white text-center py-2.5 px-4 text-sm tracking-wide">
  <p>{t('announcement', lang)}</p>
</div>
